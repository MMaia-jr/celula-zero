# WORK PACKET — R4 GENESIS EXECUTION FAILURE / RESTART / RECOVERY V1

Status:

`HUMAN-AUTHORIZED LOCAL EXECUTION / EXECUTED / HUMAN REVIEW ACCEPTED`

Canonical execution base:

`797d0292359d6ebf1ff0034ceecdf76b0200134a`

Current strategic envelope:

`D037 / GENESIS READINESS BEFORE EXTERNAL TRANSFER`

## Provenance

A READBACK/MAP and Work Packet candidate were prepared outside the repository
after Marcos selected:

`R4 — Genesis Execution Failure / Restart / Recovery V1`

Marcos then replied:

> autorizado

in direct response to the candidate's separate execution-authorization gate.

The exact one-word Human Original Record was preserved outside the repository
before execution.

The coordinator interpretation was also preserved separately and explicitly as:

`COORDINATOR INTERPRETATION / NOT ORIGINAL RECORD`

Therefore this canonical Work Packet records the effective authorized packet as:

`R4 CANDIDATE + CONTEXTUAL HUMAN AUTHORIZATION`

Preserve:

`ORIGINAL RECORD ≠ INTERPRETATION`

`EFFECTIVE AUTHORIZED WORK PACKET ≠ ORIGINAL HUMAN AUTHORIZATION`

## Property

Test whether a real bounded execution ending in a controlled failure can be
reconstructed by a new process while preserving:

- canonical base;
- Human authorization;
- Work Packet;
- allowed repository scope;
- structured result;
- partial dirty candidate;
- changed paths;
- diff/hashes;
- non-promotion state;
- return to Human disposition;

without relying on the previous process memory or prior chat for reconstruction.

## READBACK / MAP before execution

Existing components were classified:

- canonical Git orientation:
  `ADOPT`;
- external Work Packet:
  `ADOPT / MAP`;
- isolated Git worktree:
  `ADOPT`;
- normal `FAILED` Execution Fabric result:
  `ADOPT`;
- fresh-process reconstruction:
  `COMPOSE`;
- Human disposition:
  `MAP / COMPOSE`.

No new recovery subsystem was justified before the experiment.

Known limits preserved:

1. diagnostic hashes are not diagnostic content;
2. a hard interruption may emit no result envelope;
3. V1 refuses dirty-worktree re-entry.

## Controlled failure execution

Authorized repository content path:

`docs/OPERATIONS.md`

Execution route:

`CANONICAL EXECUTION FABRIC → CODEX_CLI`

The task was a real documentation need:

document failure/recovery handling for the existing Execution Fabric.

The final validator intentionally exited:

`77`

after the first three substantive validations passed.

Expected result:

`FAILED / WITHIN_SCOPE`

The failure injection was only a validator failure.

It did not authorize Codex to violate scope or perform unsafe behavior.

## Required durable artifacts

Before execution:

- Human Original Record;
- authorization-context interpretation;
- READBACK/MAP record;
- exact Work Packet;
- base SHA.

After execution:

- raw stdout;
- raw stderr;
- structured `cz.execution-result.v1`;
- partial patch;
- exact candidate copy;
- worktree path;
- hashes.

## Fresh-process boundary

After Stage 1, the original process ended.

Recovery was required to start as a new process.

The recovery process was read-only with respect to repository content and used:

`0 Codex / model calls`

It was required to reconstruct:

- current canonical main;
- original canonical base;
- Human Original Record;
- interpretation class;
- exact Work Packet;
- result envelope;
- preserved worktree HEAD;
- actual changed paths;
- exact partial diff;
- exact candidate bytes;
- non-promotion state.

## Recovery acceptance criteria

A successful recovery could classify only:

`RECOVERED_FAILED_EXECUTION / PARTIAL_CANDIDATE_PRESERVED`

Required:

- result remains:
  `FAILED`;
- result scope:
  `WITHIN_SCOPE`;
- worktree HEAD remains original base;
- actual changed paths equal result changed paths;
- actual paths are within the packet allowlist;
- partial patch bytes unchanged;
- partial candidate bytes unchanged;
- no automatic retry;
- no repository content write;
- no Git promotion.

Not:

- `COMPLETED`;
- `VERIFIED`;
- `CANONICAL`;
- `RESUMED`.

## Human disposition

After exact patch readback, Human Review adopted:

`REBUILD_FROM_CLEAN_BASE`

The failed partial candidate remains evidence.

It is not treated as a valid execution or promoted directly.

Preserve:

`FAILED RESULT + DIRTY CANDIDATE ≠ SAFE RETRY`

`REBUILD FROM CLEAN BASE ≠ RETRY`

## Explicitly out of scope

- hard-kill/crash recovery;
- generic recovery state machine;
- Execution Fabric code changes;
- dependency changes;
- second repository path;
- automatic retry;
- automatic cleanup;
- automatic promotion;
- Kimi/paid review;
- Remote Supabase;
- deployment;
- funds;
- outreach;
- commit/push/PR/merge;
- R5.

## Result boundary

A PASS can demonstrate at most:

`CONTROLLED FAILED EXECUTION → NEW PROCESS → DETERMINISTIC RECONSTRUCTION → HUMAN DISPOSITION`

Preserve:

`CONTROLLED FAILURE RECOVERY N=1 ≠ GENERAL RECOVERY`

`RECOVERY ≠ RETRY`

`RECOVERY ≠ PROMOTION`

END
