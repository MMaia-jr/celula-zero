# D020 — Human adopts Karabirrdt 001 with Human Review corrections

Class:

`DECISION / HUMAN DIRECTION`

Authority:

`HUMAN / MARCOS`

Date:

`2026-09-06`

Canonical base before promotion:

`5cb4675577a9c2ee2c83b6f940f6453b8fdc78f8`

## Provenance

Karabirrdt Circle 001 produced a local, non-canonical candidate map using Kimi
K2.6. Deterministic validation found two structural identifier/reference errors.
Human Review then examined the substantive map, corrected its phase/authority
boundaries, reduced internal overbuilding, mapped existing assets, and separated
Doing from Celebration/review.

The AI-authored candidate and Human Review are advisory until Human adoption.

Human Original Record adopting the corrected Karabirrdt:

> Adoto o Karabirrdt 001 com as correções do Human Review: autorização de Doing passa a ser Human Gate e não task; T006a é preservada como rota ativa de descoberta de oportunidade, sem número obrigatório de contatos e sem bloquear oportunidade espontânea; tarefas internas tornam-se suporte/calibração e não pré-condições absolutas da experiência externa; T004 vira MAP/CONDITIONAL EXTEND e T005 fica DEFER/NON-BLOCKING; capacidades e o WP-HA-001 existentes devem ser adotados/mapeados antes de construir nova entrada; a experiência externa e sua avaliação/celebração são separadas para preservar resultado, evidência, verificação, Human Review e aprendizagem. Atualizo D016 para que internal N=1 + baseline deixem de ser bloqueadores absolutos da descoberta/experiência externa, permanecendo como disciplina de readiness/calibração quando material. Habitabilidade N=1 exige resultado observável ligado à necessidade real, indicação direta de benefício pelo participante, revisão humana e aprendizagem preservada. Doing continua não autorizado até gate humano explícito.

Preserve:

`AI candidate ≠ Human-adopted Karabirrdt`

`Human Review interpretation ≠ Human Direction until adopted`

`Karabirrdt task ≠ Work Packet ≠ execution ≠ evidence`

`Human Gate ≠ task`

## Human-adopted bounded project

The bounded project remains:

`current canonical state → real external need/opportunity → concrete intervention → observable result → participant benefit indication → Human Review → material learning preserved`

Preserve:

`O1–O6 = sufficient enabling conditions / constraints ≠ completion prerequisites`

`O7 = property directly tested`

`H1 = invariant`

`external habitability N=1 ≠ adoption ≠ PMF ≠ scale`

## Human-adopted corrected Karabirrdt 001

### G1 — Human Gate: bounded Doing authorization

Class:

`HUMAN GATE / NOT A TASK`

A legitimate Human decision is required before any bounded Doing execution,
external participant enrollment/contact when it constitutes execution, or task
execution.

Preserve:

`authorization ≠ task`

`AI recommendation ≠ authority`

`Doing remains closed until explicit contextual Human authority`

### K1 — Minimum readiness sufficient for N=1

Class:

`PLANNING / INTERNAL / SUPPORTING-CONDITIONAL`

Use existing capabilities to check only the readiness properties materially
needed for the selected N=1 path.

This is not a requirement to complete O1–O6 globally.

`ADOPT / MAP` before `EXTEND / MISSING`.

### K2 — Simple baseline calibration

Class:

`PLANNING / INTERNAL / SUPPORTING-CALIBRATION`

When material to the selected external case, preserve the smallest useful
counterfactual/baseline: what the person would likely do without Célula Zero and
what outcome would count as useful.

This remains a discipline of comparison, not an absolute gate that must be
completed before discovering an opportunity.

### K3 — Select one real opportunity / need

Class:

`PLANNING / BOUNDARY`

Identify at least one sufficiently plausible real person + real
need/problem/opportunity for Human Review and selection.

The raw `T006a` idea is preserved here as an active zero-build discovery route,
but:

- no fixed number of candidate contacts is required;
- an inbound/spontaneous opportunity may satisfy the outcome;
- proactive outreach or enrollment that crosses into execution retains its
  applicable Human gate;
- discovery must not become a reason to build a new entry surface.

### K4 — Map/adapt existing external-run capability

Class:

`PLANNING / BOUNDARY`

Before building new entry capability, map/adopt the existing
`WP-HA-001-FIRST-EXTERNAL-RUN.md` and other current capabilities sufficient for
the selected case.

Preserve, as applicable:

- consent;
- privacy/LGPD minimization;
- ordinary communication channels first;
- STOP gates;
- baseline/counterfactual;
- missing-property gate;
- de-identified Result Package.

`WP-HA-001` is an existing scaffold, not an automatic replacement for the newer
D018/D019/D020 Human Direction or its N=1 success boundary.

### K5 — Real intervention and observable result

Class:

`DOING / EXTERNAL`

After G1 authorization, perform only the bounded intervention required by the
selected real need/opportunity.

Observe the result without collapsing:

`activity ≠ contribution ≠ result ≠ evidence ≠ verification`

No new feature is implemented inside the run merely to avoid a STOP.

If a blocking property is observed:

`ADOPT / MAP / EXTEND / MISSING`

and preserve the concrete property-loss statement before proposing
implementation.

### K6 — Participant feedback + evaluation/review

Class:

`CELEBRATING / BOUNDARY`

Separate the external encounter from its evaluation.

Candidate external habitability N=1 requires:

1. a real need/problem/opportunity;
2. a concrete Célula Zero intervention;
3. an observable result tied to that need;
4. direct participant indication of whether benefit was received;
5. reconstructibility sufficient for review;
6. Human Review classification:
   `PASS / FAIL / PARTIAL / INCONCLUSIVE`.

No third-party proof or behavior-change claim is required unless a stronger
claim is being made.

### K7 — Preserve material learning / reconcile state

Class:

`CELEBRATING / INTERNAL`

Preserve only material learning needed for future action and, when authorized,
perform the smallest canonical reconciliation.

Use existing structures first:

- Result Package for the episode;
- Decision/Human Direction only where a legitimate decision occurred;
- PROTOCOL/regression/preflight only for generalized reusable consequences;
- STATE only for short current operational consequence;
- Git history for lineage.

No new memory system is implied.

## Non-blocking / conditional work

Raw candidate `T004 — Operational Learning Retention` becomes:

`MAP EXISTING / CONDITIONAL EXTEND / NOT CURRENT CRITICAL PATH`

D016 and existing operational-learning structures already preserve the rule.
Extend only when a new incident demonstrates a concrete lost property.

Raw candidate `T005 — O3 Sufficiency Test` becomes:

`DEFER / NON-BLOCKING`

Dreaming v2 already demonstrated explicit AI→AI question exchange N=1, while
Planning Circle 001 and Karabirrdt Circle 001 created zero AI→AI questions.
Do not spend merely to manufacture dialogue. Reopen only if real external work
exposes a coordination property loss.

New external entry surface remains:

`ADOPT EXISTING CHANNEL FIRST`

`NO NEW SURFACE WITHOUT OBSERVED PROPERTY LOSS`

## Songline / dependency discipline

Primary bounded songline:

`K3 → K4 → G1 → K5 → K6 → K7`

K1 and K2 feed G1 only when material to the selected case:

`K1 readiness ─┐`

`K2 baseline  ─┼→ G1`

`K3 → K4 ─────┘`

They are not global completion prerequisites.

A spontaneous/inbound opportunity can satisfy K3 without first executing a
proactive scout campaign.

## Refinement of D016 sequencing

D016 is preserved as historical Human Direction and is not rewritten.

D016 originally established:

`CLEAN HABITABLE INTERNAL N=1 → SIMPLE BASELINE N=1`

D020 refines the immediate sequencing for the current D018/D019 bounded project:

- internal N=1 and simple baseline are not absolute blockers to discovering or
  selecting an external opportunity;
- they remain readiness/calibration disciplines when material to the selected
  external case;
- internal preparation does not prove external habitability;
- a real external run still requires its applicable Human authorization,
  consent/privacy boundaries and reconstructibility;
- observed external friction may reactivate internal work only through a concrete
  property-loss statement.

Preserve:

`REFINED ≠ ERASED`

`older Human Direction ≠ false`

`newer Human Direction governs current sequencing`

## Relationship to existing WP-HA-001

`WP-HA-001-FIRST-EXTERNAL-RUN.md = ADOPT/MAP EXISTING SCAFFOLD`

Use its useful existing preparation rather than recreating a parallel framework.

However:

`older WP success criteria ≠ automatically current D020 success criteria`

The current N=1 boundary is governed by the latest Human Direction.

## Current phase consequence

After Human adoption:

`DREAM = HUMAN ADOPTED / D018`

`GOAL + OBJECTIVES O1–O7 = HUMAN ADOPTED / D019`

`KARABIRRDT 001 = EXECUTED_LOCAL / HUMAN REVIEWED / HUMAN ADOPTED / D020`

`PLANNING = HUMAN ADOPTED / READY FOR CANONICAL RECONCILIATION`

`DOING = NOT AUTHORIZED`

`TASK EXECUTION = NOT AUTHORIZED`

`REMOTE SUPABASE = NO`

## Next gate after canonical reconciliation

`HUMAN REVIEW / AUTHORIZE FIRST BOUNDED DOING`

The next Human gate may authorize only the smallest bounded action justified by
the selected opportunity.

No Codex, Kimi Code, paid model call, outreach, external enrollment,
implementation or Doing is implied by D020 alone.

## Promotion authority

Human Original Record authorizing promotion of exactly D020 + reviewed Result
Package + STATE consequence:

> Autorizo promover D020 + Result Package do Karabirrdt 001 + STATE.md, com commit, push, PR e merge se os prechecks e o diff passarem sem mudança de escopo; sem calls pagas, sem Remote Supabase e sem iniciar Doing.

Promotion boundary:

`COMMIT = AUTHORIZED`

`PUSH = AUTHORIZED`

`PR = AUTHORIZED`

`MERGE = AUTHORIZED IF PRECHECKS + DIFF PASS WITHOUT SCOPE CHANGE`

`PAID MODEL CALLS = NOT AUTHORIZED`

`REMOTE SUPABASE = NOT AUTHORIZED`

`DOING = NOT AUTHORIZED`

`TASK EXECUTION = NOT AUTHORIZED`
