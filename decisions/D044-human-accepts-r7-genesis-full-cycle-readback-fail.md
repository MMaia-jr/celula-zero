# D044 — Human accepts R7 Genesis Full-Cycle Readback failure

Class:

`DECISION / HUMAN DIRECTION`

Decision subtype:

`HUMAN REVIEW / FAIL ACCEPTANCE + CANONICAL PROMOTION AUTHORIZATION`

Authority:

`HUMAN / MARCOS`

Date:

`2026-09-21`

Current strategic envelope:

`D037 / GENESIS READINESS BEFORE EXTERNAL TRANSFER`

## Human Review Original Record

Marcos explicitly stated:

> Aceito `R7 — Genesis Full-Cycle Readback` como `FAIL / LOCAL / READ-ONLY / BOUNDED / NO EXISTING REAL FULL-CYCLE EPISODE EXPLICITLY RECONSTRUCTIBLE / PROVENANCE GAPS OBSERVED`. O único Company Core real do Founder preserva explicitamente Company Core → DragonCycle → Need e um AI Run failure-path, mas não preserva Result, Evaluation ou Company Core Consequence; o DragonCycle permanece `DREAMING / OPEN` sem Human Direction, Plan, Do ou Celebration records; não existe `Need→Opportunity` para a rota K002 e não existe `DragonCycle→AgentTask/AgentExecution` para a rota T3 alternativa. Registro que isso demonstra perda de reconstructibilidade end-to-end no episódio vivido, não ausência das capacidades de schema necessárias e não falha geral da Célula Zero. Não autorizo preenchimento retroativo dos elos ausentes. `EXTEND` não é justificado por R7. Genesis full internal readiness permanece não demonstrada e external transfer continua `HOLD`.

This is the Human Review acceptance of the R7 result.

Accepted result:

`R7 — GENESIS FULL-CYCLE READBACK = FAIL / LOCAL / READ-ONLY / BOUNDED / NO EXISTING REAL FULL-CYCLE EPISODE EXPLICITLY RECONSTRUCTIBLE / PROVENANCE GAPS OBSERVED`

## Accepted property result

R7 tested whether one existing real Founder episode could be reconstructed
end-to-end using only already-preserved canonical relations and states, without
creating records, inferring semantic links from project/cell co-residence,
temporal proximity or textual similarity, or broadening read authority.

The tested property failed for the observed real Founder episode.

Observed explicit root:

`COMPANY CORE → DRAGON CYCLE → NEED`

Observed terminal Company Core / AI failure path:

- Company Core cycle:
  `7df3adbe-34a1-44bf-ab6c-85c743f0ed83`;
- Dragon cycle:
  `d807f305-cf3f-425c-81a5-ca368ffda874`;
- Need:
  `319ef790-d3f9-47e1-b337-de9259d066e5`;
- AI Run:
  `b244afe2-5951-44e7-8669-6d5906b451dd`;
- Company Core:
  `AI_FAILED`;
- Agreement:
  `DEFINED`;
- Result:
  `ABSENT`;
- Evaluation:
  `ABSENT`;
- Company Core Consequence:
  `ABSENT`;
- DragonCycle:
  `DREAMING / OPEN`;
- Human Direction:
  `ABSENT`;
- Human Dream / Plan / Do / Celebration records:
  `0 / 0 / 0 / 0`.

Preserve:

`R6 FAILURE-PATH ECONOMIC READINESS PASS ≠ R7 FULL-CYCLE RECONSTRUCTIBILITY PASS`

`R7 FAIL ≠ CÉLULA ZERO FAILURE`

`R7 FAIL ≠ ABSENCE OF EXISTING SCHEMA CAPABILITIES`

`R7 FAIL ≠ R1–R6 INVALIDATED`

## Stage results accepted

### Stage 0 — integrated metabolism readback

After canonical workspace and schema-parity prechecks passed:

- canonical migrations:
  `49 / PRESENT`;
- accepted adjacent local migrations:
  `7 / EXACT IDENTITIES + FINGERPRINTS`;
- critical function-body parity:
  `PASS`;
- required canonical readback surfaces:
  `PASS`;
- unique Founder relational resolution:
  `PASS`;
- existing `k002_metabolism_episodes`:
  `0`.

Result:

`BLOCKED / STAGE 0 / NO_EXISTING_METABOLISM_EPISODE / LOCAL / READ-ONLY / BOUNDED`

This did not establish absence of underlying records or failure of K002.

### Stage 0B — K002 precomposition candidate readback

Observed:

- `Company Core → DragonCycle → Need`:
  `1 explicit path`;
- `Need → Opportunity`:
  `0`;
- downstream explicit K002-precomposition chain:
  `0`;
- same-project Commitment fallback:
  `0`;
- context-only records promoted to provenance:
  `NO`.

Result:

`BLOCKED / ZERO K002-PRECOMPOSITION FULL CHAIN / FIRST ABSENT EDGE = NEED→OPPORTUNITY`

This result was not generalized to all possible full-cycle routes.

### Stage 0C — alternate explicit T3 composition readback

Starting only from the exact real Company Core / DragonCycle:

- `PLANS → AgentTask` bindings:
  `0`;
- `RESULT_OF → AgentExecution` bindings:
  `0`;
- reachable AgentExecutions:
  `0`;
- completed reachable AgentExecutions:
  `0`;
- canonical input/result Artifact pairs:
  `0`;
- downstream Claim / Evidence / Verification / Human Decision route:
  `0`;
- context-only same-project executions:
  `0`;
- context-only same-project Artifact links:
  `0`.

Result:

`BLOCKED / ZERO ALTERNATE EXPLICIT FULL ROUTE`

Observed property loss:

`NO_EXPLICIT_DRAGON_TO_AGENT_EXECUTION_ROUTE`

After Stage 0B and Stage 0C, no authorized explicit end-to-end reconstruction
path remained for this real Founder episode.

## Epistemic boundary

R7 does not infer that no other historical records exist somewhere.

R7 establishes the narrower operational result that the tested real Founder
episode cannot be reconstructed end-to-end through the canonical explicit
relations required by this slice.

Preserve:

`CO-RESIDENCE IN PROJECT / CELL ≠ PROVENANCE`

`TEMPORAL PROXIMITY ≠ PROVENANCE`

`TEXTUAL SIMILARITY ≠ PROVENANCE`

`RETROACTIVE REPRESENTATION ≠ EVIDENCE THAT THE LIVED EPISODE HAD THAT REPRESENTATION`

`FULL-CYCLE READBACK ≠ FULL-CYCLE SUCCESS`

## Construction consequence

R7 found no evidence that a new schema primitive, ontology, orchestrator,
metabolism table, Agent bridge or generic protocol is required.

The repository already contains primitives for:

- Need → Opportunity;
- DragonCycle → AgentTask / AgentExecution;
- Execution → Artifact;
- Artifact / Contribution → Claim;
- Claim → Evidence;
- Evidence → Verification;
- Verification → Human Decision;
- Dragon method records for Human Direction / Plan / Celebration;
- Company Core Result / Evaluation / Consequence.

The observed loss is in the lived episode's use and representation through
those existing relations, not in an established absence of schema capability.

Classification:

`ADOPT / MAP = EXISTING CAPABILITY`

`COMPOSE = NOT RETROACTIVELY AUTHORIZED`

`EXTEND = NOT JUSTIFIED BY R7`

`NEW INFRASTRUCTURE = NOT JUSTIFIED BY R7`

No retroactive fill of missing relations is authorized.

## Readiness consequence

The D037 ladder includes full-cycle readback as the final internal evidence
step before a separate Genesis Readiness Review.

R7 failed that property.

Therefore:

`GENESIS FULL INTERNAL READINESS = NOT DEMONSTRATED`

External transfer remains:

`HOLD UNTIL GENESIS READINESS REVIEW + SEPARATE HUMAN AUTHORIZATION`

No R8 or replacement slice is selected by this Decision.

No new prospective episode is authorized by this Decision.

## Canonical promotion Original Record

Marcos explicitly authorized:

> Autorizo preparar o pacote canônico de fechamento de R7 limitado a `D044`, `RP-R7-GENESIS-FULL-CYCLE-READBACK-V1.md` e `STATE.md`, sobre o HEAD canônico `4729e41069f71ceb92058f85636edfd76ff73797`, preservando exatamente a decisão humana acima e sem selecionar R8 ou autorizar novo episódio. Autorizo commit, push, abertura de PR e merge somente desses três arquivos, desde que prechecks, diff e escopo sejam exatos e nenhuma outra alteração seja incluída. Qualquer divergência deve STOP.

Authorized repository scope:

1. `decisions/D044-human-accepts-r7-genesis-full-cycle-readback-fail.md`
2. `RP-R7-GENESIS-FULL-CYCLE-READBACK-V1.md`
3. `STATE.md`

Authorized promotion:

`COMMIT → PUSH → PR → MERGE IF PRECHECKS + DIFF + EXACT SCOPE MATCH`

Not authorized:

- any code or schema change;
- DB write or retroactive episode composition;
- Remote Supabase write;
- model/provider call or spend;
- deploy;
- outreach;
- R8 selection;
- new prospective episode.

STOP if canonical base or exact package scope changes before merge.

END
