# D050 — Human selects Founder Online Habitable MVP Assembly as first lived-capacity episode

Class:

`DECISION / HUMAN DIRECTION`

Authority:

`HUMAN / MARCOS`

Date:

`2026-09-23`

Status:

`HUMAN DIRECTION EXPRESSED / DOCUMENTATION PROMOTION AUTHORIZED / CANONICAL ONLY AFTER MERGE`

Canonical base inspected:

`605f5ef285fe6c069a8e384a5e16edf15ec2e914`

Promotion authority:

`AUTHORIZED BY HUMAN / D050 + STATE RECONCILIATION + WP-K03 / BRANCH + COMMIT + PUSH + PR + MERGE`

## Human Direction Original Record

Marcos explicitly stated:

> Seleciono como primeiro lived-capacity episode a criação de uma experiência online habitável para eu trabalhar de verdade na Célula Zero. Autorizo preparar um bounded MVP Assembly Work Packet para Codex, priorizando tecnologias e capacidades existentes e proibindo nova infraestrutura sem property loss observada.

## Selected first lived-capacity episode

Adopt as K03:

`FOUNDER ONLINE HABITABLE MVP ASSEMBLY`

Human problem:

Célula Zero already contains substantial technical and operational capability,
but Marcos does not yet experience it as one coherent online environment where
he can work for real without repeatedly becoming the manual integration layer
between subsystems, terminals, internal architecture and external assistants.

Target transformation:

`FRAGMENTED CAPABILITIES + FOUNDER AS MANUAL BUS`

→

`COHERENT ONLINE FOUNDER WORKSPACE + REUSABLE LIVED EPISODE`

The selected episode is not:

- a greenfield rebuild;
- a request to replace working primitives;
- a frontend-only redesign;
- a license to add infrastructure speculatively;
- an external-user episode;
- an Allus transfer episode.

## Relationship to D049

D049 remains the current critical-path direction.

D050 selects the first real episode under D049.

Apply:

`REAL FOUNDER LIVED-CAPACITY EPISODE`
`→ COMPOSE EXISTING CAPABILITY`
`→ OBSERVE CONSEQUENCE / CAPACITY DELTA / PROPERTY LOSS`
`→ ADOPT / MAP / COMPOSE`
`→ EXTEND only when concrete property loss requires it`
`→ HUMAN REVIEW`

Construction tie-breaker remains:

`ADOPT → MAP → COMPOSE → EXTEND`

For any `EXTEND / MISSING`, require:

`OBSERVED BLOCKAGE`
`→ LOST USER POSSIBILITY / PROPERTY`
`→ WHY ADOPT FAILED`
`→ WHY MAP FAILED`
`→ WHY COMPOSE FAILED`
`→ SMALLEST EXTENSION`
`→ ACCEPTANCE TEST`

Without that chain:

`DO NOT BUILD`

## Existing-technology direction

The first episode must aggressively inspect and reuse mature existing
capabilities before custom construction.

This includes both:

1. existing Célula Zero capabilities already represented in the repository; and
2. maintained external technologies that preserve the required Human authority,
   provenance, privacy, portability, licensing and exit properties.

Known existing candidates to evaluate include, without automatically adopting:

- current Next.js / React web application;
- Supabase;
- Vercel hosted path;
- GitHub canonical software/history;
- current AI gateway/runtime;
- durable AI Job / AI Run capability;
- Execution Fabric;
- Task Capsule / Result-return paths;
- Company Core;
- Operating Loop / Workbench;
- Genesis;
- Huly as an existing collaboration/project substrate candidate.

Preserve:

`MATURE EXISTING TECHNOLOGY ≠ AUTOMATIC ADOPTION`

`BOUNDED HULY PASS ≠ HULY UNIVERSAL SUBSTRATE`

`NOT DISCOVERED YET ≠ MISSING`

## Prepared Work Packet

Authorized preparation:

`WP-K03-FOUNDER-ONLINE-MVP-ASSEMBLY-V1.md`

Prepared status:

`BOUNDED / EXECUTOR CANDIDATE = CODEX / NOT AUTHORIZED FOR EXECUTION`

The Work Packet is intended to direct Codex to inspect the repository as an
existing product, produce a reuse/adoption map first, and assemble one coherent
vertical Founder experience rather than another isolated primitive.

## Intended product property

The target Founder journey is substantially:

`EXPRESS`
`→ UNDERSTAND / CORRECT`
`→ DISCOVER`
`→ CHOOSE`
`→ COMPOSE`
`→ AUTHORIZE`
`→ ACT`
`→ RECEIVE RESULT / CONSEQUENCE`
`→ HUMAN DECISION`
`→ CAPACITY-DELTA / LEARNING`
`→ RETURN WITH CONTINUITY`

This journey may loop and does not require these internal terms to be exposed in
the UI.

Success requires product coherence, not merely component correctness.

## What this Human Direction authorizes now

Authorized before promotion:

- preparation of the bounded MVP Assembly Work Packet;
- repository readback and planning necessary to prepare that packet;
- preservation of K03 selection in a candidate canonical reconciliation
  package.

Subsequent Human documentation-promotion authorization:

> Autorizo promover D050, a reconciliação de STATE e o WP-K03 ao GitHub canônico, incluindo branch, commit, push, PR e merge. O WP-K03 entra como PREPARED / NOT EXECUTED. Codex execution, implementação, deploy, Remote Supabase, paid calls, external transfer e outreach continuam HOLD.

Authorized promotion package:

- `decisions/D050-human-selects-founder-online-habitable-mvp-assembly.md`;
- `STATE.md`;
- `WP-K03-FOUNDER-ONLINE-MVP-ASSEMBLY-V1.md`.

Authorized promotion operations:

`BRANCH + COMMIT + PUSH + PR + MERGE`

## What this Human Direction does NOT authorize

Not authorized:

- Codex execution;
- repository implementation/code changes beyond this documentation package;
- new dependencies;
- schema change;
- migration;
- Remote Supabase write;
- Vercel/deployment change;
- model/provider spend;
- new external infrastructure;
- Huly deployment or Huly core modification;
- external participant;
- Allus;
- outreach;
- funds.

A separate explicit Human execution authorization is required before Codex may
implement the MVP.

Preserve:

`DOCUMENTATION PROMOTION AUTHORIZED ≠ CODEX EXECUTION AUTHORIZED`

## External transfer

External transfer remains:

`HOLD`

Preserve:

`FOUNDER HABITABILITY ≠ EXTERNAL UTILITY`

`FOUNDER N=1 ≠ ADOPTION ≠ PMF ≠ SCALE`

## Next Human gate

The Work Packet is preserved canonically as:

`PREPARED / BOUNDED / NOT EXECUTED / EXECUTION NOT AUTHORIZED`

Next gate:

`HUMAN EXECUTION DECISION FOR WP-K03-FOUNDER-ONLINE-MVP-ASSEMBLY-V1`

Possible dispositions:

`AUTHORIZE EXECUTION / REVISE / HOLD / STOP`

If execution is authorized, it must remain bounded by:

- exact canonical base;
- isolated workspace/branch;
- Stage 0 readback and reuse map before writes;
- STOP gates for new infrastructure or expanded authority;
- no repository promotion without a later Human gate.

Preserve:

`K03 SELECTED ≠ WP PREPARED ≠ EXECUTION AUTHORIZED ≠ MVP BUILT ≠ VERIFIED ≠ HUMAN-ACCEPTED`

`D050/WP CANONICAL DOCUMENTATION ≠ MVP IMPLEMENTED`

END
