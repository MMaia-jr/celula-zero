# D017 — AI Dragon Circle v2 Dreaming authorization

Class:

`DECISION / HUMAN DIRECTION`

Authority:

`HUMAN / MARCOS`

Date:

`2026-09-06`

Canonical base at authorization time:

`59627f582530a147eed04794d23c3d6d3ff8e4d0`

## Human authorization

Exact Human Original Record:

> Autorizo aplicar os artefatos de aprendizagem e o Work Packet ao repositório,
> commit, push, abrir PR e merge se os prechecks/diff passarem sem mudança de
> escopo; e autorizo até US$2 para executar o AI Dragon Circle v2 em Dreaming,
> parando antes de Planning.

## Direction

The immediate bounded experiment is:

`AI DRAGON CIRCLE v2 / DREAMING ONLY`

Authorized execution envelope:

`PAID AI GATEWAY HARD CEILING = USD 2.00`

The experiment must stop before Planning.

The Orchestrator may manage ordinary Dreaming turns, questions, bounded shared
context and resource allocation inside this authorized phase envelope.

The Orchestrator does not acquire authority to:

- redefine Human Direction;
- move to Planning;
- expand the USD 2.00 ceiling;
- deploy;
- write Remote Supabase;
- promote new implementation/results automatically.

## Promotion authority for the learning package

For the bounded artifacts covered by this decision:

`APPLY = AUTHORIZED`

`COMMIT = AUTHORIZED`

`PUSH = AUTHORIZED`

`PR = AUTHORIZED`

`MERGE = AUTHORIZED IF PRECHECKS + DIFF PASS WITHOUT SCOPE CHANGE`

This promotion authority does not automatically promote later experiment
results or implementation.

## Preserve

`phase budget authority ≠ unrestricted spend authority`

`facilitation ≠ Human Direction`

`Dreaming execution ≠ Planning authorization`

`candidate mechanism ≠ demonstrated architecture`

## Non-inferences

This decision does not establish:

- a general multi-agent architecture;
- a general memory architecture;
- RAG/vector/graph necessity;
- streaming cancellation semantics;
- external utility;
- adoption;
- PMF;
- scale.
