# D021 — Internal operability before external Doing

Class:

`DECISION / HUMAN DIRECTION`

Authority:

`HUMAN / MARCOS`

Date:

`2026-09-07`

Canonical base before promotion:

`b298798c743185eaa58c6857ebb93c9cea216ef5`

## Decision

Célula Zero deve resolver primeiro os problemas materiais de operabilidade
interna já demonstrados antes de retomar a execução imediata da trilha externa
K3/K4.

Isto refina a sequência imediata. Não abandona D020, Dualite, Future Readiness
nem a busca de utilidade externa.

Preserve:

`REFINED ≠ ERASED`

`internal operability first ≠ internal perfection first`

`internal preparation ≠ external utility`

`Dualite preserved ≠ Dualite selected for immediate execution`

## Why this direction exists

O trabalho recente demonstrou que a Célula Zero já possui capacidades reais,
mas algumas ainda dependem de conhecimento histórico, composição manual ou
contratos operacionais frágeis.

A propriedade relevante não é produzir mais primitives. É permitir que uma
pessoa tecnicamente competente consiga descobrir, executar, interromper,
retomar e verificar os caminhos legítimos sem memória oculta do fundador.

For each material gap:

`criterion → inspect existing capability → falsify loss → smallest repair if required → deterministic test → Human Review`

Before technology:

`ADOPT → MAP → EXTEND / MISSING only on concrete property loss`

## First completed consequence

Founder canonical bootstrap:

`WP-GI1-001 / PR #165 = MERGED / CANONICAL`

Observed after Human Review:

- canonical repository identity is verified;
- actual remote `main` is resolved;
- stale local tracking state is rejected;
- canonical `STATE.md` is read by verified SHA;
- current Human Direction and next gate are parsed from canonical state;
- unresolved canonical controls fail closed before gateway/provider activity;
- unrelated authority blockers do not become an unintended global shutdown.

Preserve:

`14/14 focused tests PASS ≠ universal Founder correctness`

`canonical bootstrap repaired ≠ internal operability complete`

## Current next property to falsify

Company Core staged headless operation.

Required bounded path:

`authenticated Human → controlled PERSON Actor → PRIVATE Project → Company Core Need → Agreement → AGREEMENT_DEFINED → STOP`

The purpose is not to test the frontend.

The purpose is to determine whether the existing Company Core can be operated
safely headlessly through the pre-AI planning boundary without manual hidden
plumbing.

Required boundaries:

- no frontend requirement;
- no AI work authorization;
- no implemented-system model call;
- no paid provider call;
- no Remote Supabase write;
- no outreach;
- no external Doing;
- no automatic transition beyond `AGREEMENT_DEFINED`.

Current allowed move:

`MAP EXISTING CAPABILITY → FALSIFY MISSING PROPERTY`

If the property is already preserved:

`ADOPT / MAP / DO NOT BUILD`

If a concrete property remains lost:

`STOP → HUMAN REVIEW → authorize or reject smallest EXTEND`

## Later preserved material gap

Move2 ambiguous-job reconciliation disposition remains material but is not the
current execution target.

Preserve:

`detection of ambiguity ≠ deterministic disposition of ambiguity`

No automatic retry agent, daemon, generic orchestrator or new platform is
implied.

## Relationship to D020 / Dualite

D020 remains valid and preserved.

Its external songline remains:

`K3 → K4 → G1 → K5 → K6 → K7`

D021 changes only current immediate sequencing.

Dualite remains:

`STRONG K3 CANDIDATE / PRESERVED / NOT CURRENT IMMEDIATE EXECUTION`

When the internal operability gate is sufficiently resolved for the intended
path, the project may return to D020's external planning without inventing a new
external-entry system.

External Doing remains closed until its applicable Human authorization.

## Current Human gate

Planning may inspect and map existing Company Core capability without crossing
into implementation or Doing.

If that inspection still demonstrates a concrete headless-operability property
loss, the next Human gate is:

`GI1-002 / HUMAN REVIEW / AUTHORIZE SMALLEST COMPANY CORE HEADLESS REPAIR IF PROPERTY LOSS REMAINS`

D021 by itself does not authorize that repair.

## What D021 does NOT authorize

D021 does not authorize:

- Company Core implementation;
- frontend work;
- paid model/provider calls;
- Remote Supabase writes;
- external outreach or participant enrollment;
- K5 Doing;
- Move2 reconciliation implementation;
- generic documentation rewrite;
- new orchestration, memory, protocol or platform primitives.

## Promotion authority

Human authorization for this exact reconciliation:

> Autorizo D021 + STATE + atualização do teste Founder correspondente, exatamente 3 arquivos, commit, push, PR e merge se todos os testes e prechecks passarem sem mudança de escopo.

Promotion boundary:

`FILES = EXACTLY 3`

`COMMIT = AUTHORIZED`

`PUSH = AUTHORIZED`

`PR = AUTHORIZED`

`MERGE = AUTHORIZED IF ALL PRECHECKS + TESTS PASS WITHOUT SCOPE CHANGE`

`MODEL CALLS = 0`

`REMOTE SUPABASE = 0`
