# PROTOCOL Candidate Delta — Multi-AI Facilitated Execution

Class:

`PROPOSED GENERALIZED LEARNING / CANONICAL CANDIDATE IF MERGED / NOT PROTOCOL RULE`

This file is intentionally NOT a direct edit to `PROTOCOL.md`.

Promotion should occur only after Human Review decides which learning has become
general enough to be a durable operating rule.

Candidate additions under operational learning retention:

1. `RETURNED ≠ COMPLETE`
2. `STORED ≠ RETRIEVED ≠ SELECTED ≠ INJECTED ≠ UNDERSTOOD`
3. `RAW TURN ≠ SELF-SUMMARY ≠ FACILITATOR GIST ≠ SHARED WORKING CONTEXT`
4. `HARD MONETARY BUDGET ≠ MAX OUTPUT TOKENS`
5. paid-call accounting should be captured before downstream task/format
   validation can reject the output;
6. deterministic presentation-format failure should not be confused with
   substantive-output failure;
7. AI facilitation may administer process/resource boundaries but does not create
   Human Direction;
8. repeated paid retries caused only by over-tight output contracts are an
   operational anti-pattern.

Candidate experiment-only conventions — NOT yet Protocol rules:

- talking-piece round robin;
- AI→AI question queue;
- Turn Cards;
- streaming/cancellation;
- phase-level budget governor;
- all-pass Dream closure.

These require the next bounded experiment before promotion as durable rules.
