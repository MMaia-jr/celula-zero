# RP-DREAM3-STATE-RIVAL-EXPERIMENT-001

Result classification:

`PASS N=1`

Execution-time promotion state:

`PREPARED ≠ COMMITTED ≠ PUSHED ≠ MERGED ≠ CANONICAL`

This package records an experiment completed before Git promotion. Git history,
not the execution-time promotion state above, determines whether this document
is later stored canonically.

Dream:

`Dream 3 — Célula Zero opera Célula Zero`

Cycle:

`a3d1f512-7595-48f4-8266-56cea9c9869d`

Canonical base tested:

`d79e4ac906bd546014fcb5fbc26270a28452c65d`

## Human Direction

Original Record:

`3c770124-6ffa-41d9-8002-d40a033244d5`

> Dream 3 deve reduzir o trabalho operacional humano que não exige autoridade, julgamento ou entrada do mundo real, sem reduzir a participação humana legítima. Vamos distinguir HUMAN_DECISION_TIME, HUMAN_WORLD_INPUT_TIME e HUMAN_PLUMBING_TIME. Antes de construir Mission Runner, cz-do, orchestrator ou nova infraestrutura, vamos executar um único experimento real comparando o menor caminho baseado na infraestrutura existente com o rival convencional mais simples e automaticamente preparado. Construiremos somente a propriedade concreta que o rival não conseguir preservar. O primeiro caso real será preparar uma candidata verificada de reconciliação do STATE.md para o estado atual do Dream 3, sem promoção Git automática.

## Real task

Prepare a correct candidate reconciliation of `STATE.md` for current Dream 3
state without automatic Git promotion.

## Compared paths

Path A:

`canonical STATE + official Room handoff`

Path B:

`canonical STATE + deterministic minimal Markdown briefing`

Both used the same Kimi model and the same output contract.

## Observed execution

| Metric | Path A | Path B |
|---|---:|---:|
| Machine status | PASS | PARTIAL |
| Prompt bytes | 156269 | 9305 |
| Prompt tokens | 41822 | 2361 |
| Completion tokens | 1824 | 2024 |
| Latency seconds | 50.448 | 42.898 |
| Calculated cost USD | 0.047027 | 0.010339 |

Total calculated model cost:

`USD 0.057366`

Observed executor boundary:

`0 manual context relay between Path A and Path B`

The executor did not time human minutes and therefore does not invent them.

## Path B PARTIAL review

Machine result:

`PARTIAL`

All required factual checks:

`PASS`

Other forbidden overclaims:

- merge claim: `false`;
- PMF claim: `false`;
- adoption claim: `false`.

The only forbidden hit was:

`dream3_canonical_claim = true`

The candidate itself states:

> Dream 3 is local operational state and NOT GitHub canonical yet.

The previous regex searched for `Dream 3 ... canonical` while only excluding
`NOT ` immediately adjacent to the word `canonical`. In the actual sentence,
`NOT GitHub canonical` is explicitly negative but `NOT ` is not immediately
adjacent to `canonical`, producing a deterministic false positive.

Semantic review:

`NO UNNEGATED DREAM 3 CANONICALITY CLAIM FOUND`

Classification of the machine PARTIAL:

`VALIDATOR FALSE POSITIVE / NOT OBSERVED SEMANTIC PROPERTY LOSS`

## Human Decision

Original Record:

`0c123293-26f8-4312-b181-9c350c14c2b0`

> Para o caso STATE reconciliation N=1, considero que o rival simples automaticamente preparado preservou as propriedades relevantes. O `PARTIAL` do Path B decorreu de falso positivo do validator, não de perda semântica observada. Adotaremos o briefing automático mínimo como caminho atual para esta classe de tarefa, sem construir Mission Runner, cz-do, orchestrator ou nova infraestrutura. O STATE final deve usar esse caminho mínimo, com a concisão operacional observada no Path A. Qualquer EXTEND/MISSING futuro exigirá nova propriedade concreta perdida em trabalho real.

## Result

`STATE RIVAL EXPERIMENT = PASS N=1`

For this specific task:

`MINIMUM AUTOMATIC BRIEFING = SUFFICIENT N=1`

`FULL ROOM HANDOFF REQUIRED FOR STATE RECONCILIATION = FALSIFIED N=1`

`CONCRETE PROPERTY LOSS REQUIRING EXTEND/MISSING = NONE OBSERVED`

This does **not** establish that minimal briefings replace the Room for all task
classes.

## Construction consequence

Current classification:

- canonical repo / STATE: `ADOPT`;
- Room for provenance/continuity: `ADOPT`;
- automatically prepared minimal briefing for this task class: `MAP`;
- Mission Runner: `NOT JUSTIFIED`;
- `cz-do`: `NOT JUSTIFIED`;
- generic orchestrator: `NOT JUSTIFIED`;
- new context infrastructure: `NOT JUSTIFIED`;
- new `EXTEND / MISSING`: `NONE DEMONSTRATED`.

## Verification

Verification artifact:

`VERIFICATION-DREAM3-STATE-RIVAL-001.md`

Classification:

`PASS N=1 / NON-INDEPENDENT`

Verification remains distinct from Human Decision, canonicality and reputation.

## Human-time boundary

Preserve:

`HUMAN_DECISION_TIME ≠ HUMAN_WORLD_INPUT_TIME ≠ HUMAN_PLUMBING_TIME`

This run demonstrates that the two path calls, capture, diffs and deterministic
checks can occur without manual relay between the paths. It does not prove
founder-light Dream 3 end to end.

## Limitations

- `N=1`;
- one task class: STATE reconciliation;
- same model on both paths;
- no independent external verifier;
- human minutes were not timed;
- no external utility, adoption, PMF or scale tested;
- at experiment/result capture, the final STATE candidate was non-canonical;
- at experiment/result capture, no commit, push, PR or merge had occurred.

## STATE reconciliation artifact

SHA256:

`e41428188d8e9ec1a01efb349b665f4ab86594a972ade6dd6e3404b5984e8d99`

At result capture:

`PREPARED ≠ COMMITTED ≠ PUSHED ≠ MERGED ≠ CANONICAL`

That statement records the execution-time boundary and remains historical even
if this Result Package is later merged.

## Post-result operating consequence

For this task class, use the minimum automatically prepared briefing by default.
Do not extrapolate the N=1 result to other task classes.

The current operational gate belongs in `STATE.md`.
